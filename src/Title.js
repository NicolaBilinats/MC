import React, {Component} from 'react';
import Button from '@material-ui/core/Button';

class Title extends Component{
    render(){
        return (
            <div>
                <Button className="bm-burger-button">МафЦентр</Button>
            </div>
        )
    }
}
export default Title;